server:
    port: 8083

spring:
    application:
        name: statistic-service
    redis:
        host: localhost
        port: 6379
    jpa:
        hibernate:
            ddl-auto: none

management:
    security:
        sessions: never
