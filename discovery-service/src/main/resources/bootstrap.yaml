spring:
  application:
    name: discovery-service
  cloud:
    config:
      name: ${spring.application.name}
      uri: http://localhost:8888
      username: configUser
      password: configPassword